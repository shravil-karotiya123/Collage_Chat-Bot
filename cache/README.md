# Cache Directory (`cache/`)

## Purpose
The `cache/` directory holds transient computational caches, model response caches, and temporary data chunks.

## Cleanup Policy
- Files in this directory are safe to delete and will be automatically purged by utility routines when memory thresholds require cleanup.
